declare class Arr {
    /**
     * Check if the given array content the value
     * @param array array
     * @param value searched value
     */
    static includes(array: (string | number)[], value: string | number): boolean;
    /**
     * Returns the values in array a missing in array b
     * @param a first array
     * @param b second array
     */
    static missing(a: Array<string | number>, b: Array<string | number>): (string | number)[];
    /**
     * Test the uniqueness of values of an array
     * @param arr array
     * @param keys Keys when it is an array of objects
     * @param separator separator for nested properties
     */
    static unique<T = any>(arr: Array<T>, keys?: keyof T | string | Array<keyof T | string>, separator?: string | RegExp): boolean;
    /**
     * Eliminates duplicates in an array
     * @param arr array
     * @param keys Keys when it is an array of objects
     * @param separator separator for nested properties
     */
    static distinct<T = any>(
    /**
     * array
     */
    arr: Array<T>, 
    /**
     * Keys when it is an array of objects
     */
    keys?: keyof T | string | Array<keyof T | string>, 
    /**
     * Separator for nested properties
     * @default .
     */
    separator?: string | RegExp): T[];
    /**
     * Apply filters to the given array's items
     * @param array array
     * @param filters filters
     */
    static apply<T>(array: T[], filters: ((item: T) => any) | ((item: T) => any)[]): T[];
    /**
     * Join array items
     * @param array array to join
     * @param glue glue for items before the last
     * @param lastGlue glue for the last item
     */
    static join<T = any>(array: T[], glue: string, lastGlue?: string): any;
}
export default Arr;
